server {
    listen 80;
    server_name 15.164.36.109; 

    location / {
        root /usr/share/nginx/html;        # 빌드된 리액트 파일이 위치하는 경로
        try_files $uri $uri/ /index.html;  # 클라이언트 측 라우팅 지원
    }

    error_page 404 /index.html;  # 404 에러 시 index.html 반환
}
